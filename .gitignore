# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment files
.env
.env.local
.env.*.local

# Test coverage
coverage

# Artefacts Playwright (E2E) — LOT 4 vague 1
/test-results/
/playwright-report/
/blob-report/
/.playwright/
/playwright/.cache/

# Audit snapshots (reproductibles via les commandes documentées
# dans docs/AUDIT_TIER1_BUREAU_DIAGNOSTIQUE.md §5).
# La règle ne s'applique qu'au dossier `audit/` à la racine (artefacts CI).
# Le dossier `docs/audit/` (documentation pour cabinet d'audit) est versionné.
/audit/
!docs/audit/
!docs/audit/**

